Пришлось сделать продолжение первого файла, так как первый отображается не до конца.

Вопрос №5.

def version_comparer(a,b):
  a=a.split(".")
  b=b.split(".")
  a1,a2=a[0],a[1]
  b1,b2=b[0],b[1]
  if a1>b1:
    print(1)
  elif a1<b1:
    print(-1)
  else:
    if a2>b2:
      print(1)
    elif a2<b2:
      print(-1)
    else:
      print(0)
a=input()
b=input()
version_comparer(a,b)

input:
1.11
1.1
output:
1
